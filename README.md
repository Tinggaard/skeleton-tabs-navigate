# Skeleton v4 bug?

```bash
git clone https://github.com/Tinggaard/skeleton-tabs-navigate
cd skeleton-tabs-navigate
pnpm i
pnpm dev
```

## Using Skeleton v4 tabs to navigate

When navigating the tabs, SvelteKit preloads the data correctly when hovering
the links, but on click, the entire app is reloaded.
